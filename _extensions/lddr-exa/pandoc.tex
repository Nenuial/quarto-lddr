$if(highlighting-macros)$
$highlighting-macros$
$endif$

$tightlist.tex()$
$tables.tex()$
$graphics.tex()$
$citations.tex()$

$for(header-includes)$
$header-includes$
$endfor$

% Setup header and footer
\header{\includegraphics[width=$logowidth$]{$logo$}}{$doc-title$}{Nom:$if(student-name)$$student-name$$else$\hspace{4cm}$endif$}
\headrule
\footer{$sign$/\the\year}{}{\thepage/\pageref{LastPage}}
\footrule

% Simple solution environment (no box)
\unframedsolutions

% Environment that does nothing
\newenvironment{emptyenv}{}{}

% Remove examedoc solution header
\renewcommand{\solutiontitle}{}

% Prevent orphan/widow questions
\BeforeBeginEnvironment{solutionorlines}{\par\nopagebreak\minipage{\linewidth}}
\AfterEndEnvironment{solutionorlines}{\endminipage}

$if(line-numbers)$
% Package for linenumbers, with options to switch the page side
\usepackage[switch,modulo]{lineno}
$endif$